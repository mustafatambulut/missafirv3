import { IWhoWeAreLayout } from "@/app/[lang]/who-we-are/types";

const WhoWeAreLayout = ({ children }: IWhoWeAreLayout)=> {
  return <section>{children}</section>;
};

export default WhoWeAreLayout;
