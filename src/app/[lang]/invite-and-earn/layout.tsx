import { IInviteAndEarnLayout } from "@/app/[lang]/invite-and-earn/types";

const InviteAndEarnLayout = ({ children }: IInviteAndEarnLayout)=> {
  return <section>{children}</section>;
};

export default InviteAndEarnLayout;
