import React, { ReactNode } from "react";
import { get } from "lodash";
import ReduxProvider from "@/redux/provider";

import Language from "@/components/atoms/language/Language";
import Header from "@/components/molecules/header/Header";
import Footer from "@/components/molecules/footer/Footer";

import "../styles/globals.css";

export const metadata = {
  title: "Create Next App",
  description: "Generated by create next app"
};

const RootLayout = ({ children, params }: { children: ReactNode; params: { lang: string } }) => {
  return (
    <html lang={get(params, "lang")}>
      <head>
        <title>Missafirv3 Beta</title>
        <meta name="google-site-verification" content="L7xAcVXBfqNFTj_VSqCaZU-gKHXK-T7chpoLJZE3FYU" />
      </head>
      <Language lang={get(params, "lang")} />
      <body className="font-mi-sans" suppressHydrationWarning={true}>
        <ReduxProvider>
          <Header />
          <main>{children}</main>
          <Footer />
        </ReduxProvider>
      </body>
    </html>
  );
};

export default RootLayout;
